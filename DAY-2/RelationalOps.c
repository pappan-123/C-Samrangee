// 1.2 Relational Operators(>, <, >=, <=, ==, !=)
//always returns boolean value (0 or 1) where 0 is false and 1 is true
//= , == ,=== 
// = assign operator a=b, b=25
//== comparison operator a==b, 25==25
//=== strict equality operator (not available in C, used in JavaScript) a===b
// int a=20, int b=20 ("20")  a==b (true) a===b(false)

#include <stdio.h>
int main()
{
    int a=19,b=20;
    // printf("Enter the value of a and b: ");
    // scanf("%d %d",&a,&b);
    // printf("a > b: %d\n",a>b);  // 0
    // printf("a < b: %d\n",a<b);  // 1
    // printf("a >= b: %d\n",a>=b); // 0
    // printf("a <= b: %d\n",a<=b); // 1
    // printf("a == b: %d\n",a==b); // 0
    // printf("a != b: %d\n",a!=b); // 1
    return 0;
}